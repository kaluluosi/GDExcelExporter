name: 安装poetry
description: 安装并设置好poetry和其缓存

inputs:
  cache-name:
    description: '缓存名称'
    required: true


runs:
  using: "composite"
  steps:
    - name: Setup Poetry
      run: pipx install poetry
      shell: bash
    - run: poetry --version
      shell: bash